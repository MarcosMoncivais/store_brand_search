<html>
<head>
    <title>Find your kicks</title>
</head>
<body>
    <div class="container">
        <h1>Check Local Shoe Inventory Here!</h1>

        {% if stores is not empty %}
            <h3>List of all stores:</h3>
            <ul>
                {% for store in stores %}
                    <li><h4><strong><a href="/stores/{{ store.getId }}">{{ store.getName }}</a></strong></h4></li>
                {% endfor %}
            </ul>
        {% else %}
            <h3>It's lonely in here....</h3>            
        {% endif %}

        <form action="/form_store">
            <button type="submit" class="btn btn-info">Add Store</button>
        </form>

        {% if form_check == true %}
            <form action="/add_store" method="post">
                <div class="form-group">
                    <label for="type">Enter a new store name</label>
                    <input id="type" name="type" class="form-control" type="text" placeholder="Store Name" required>
                </div>
                <button type="submit" class="btn btn-success">Add Store</button>
            </form>
        {% endif %}
        <form action="/delete_stores" method="post">
            <button type="submit" class="btn btn-danger">Delete All Stores</button>
        </form>
    </div>
</body>
</html>